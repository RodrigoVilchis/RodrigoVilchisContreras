
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous")
        title = Mi plantilla de Ingreso de datos en Pug
    
    body
    div.container
        h1(style="color:blue;") Ingrese Producto
                if error
                    div(class="alert alert-danger")=error
                div(class="table table-dark")
                form(action="/productos" method="post")
                    div.form-group
                        label(for="nombre" class="form-label") Nombre
                        input(type="text" id="nombre" name="nombre"  class="form-control" value=nombre)
                    div.form-group
                        label(for="precio" class="form-label") Precio
                        input(type="number" id="precio", name="precio"  class="form-control" value=precio)
                    div.form-group
                        label(for="fotourl" class="form-label") Foto URL
                        input(type="text" id="fotourl" name="fotourl"  class="form-control" value=fotourl)
                    button(class="btn btn-success mt-3 mb-5") Enviar

    div.container
        h1 Vista de Productos
            if productos.length > 0    
                div.table-responsive
                    table(class="table table-hover")
                        thead 
                            tr
                                th Nombre
                                th Precio
                                th Foto URL
                        tbody(class="table table-dark")
                        for producto of productos
                            tr
                                td= producto.nombre
                                td= producto.precio
                                td= producto.fotourl
            else
                h3(class="alert alert-danger") No se encontraron productos

