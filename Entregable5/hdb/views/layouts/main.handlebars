<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset='UTF-8' />
    <meta http-equiv='X-UA-Compatible' content='IE=edge' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <link href='https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' rel='stylesheet'
    integrity='sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3' crossorigin='anonymous'/>
    <title>Mi plantilla de Ingreso de Datos en Handlebars</title>
</head>


<body>
    <div class="container">
            <h1 style="color:blue;">Ingrese producto</h1>
    <form action="/productos" method="post">
    <div class="form-group">
        <label for="nombre" class="form-label">Nombre</label>
        <input type="text" id="{{nombre}}" name="nombre" class="form-control"/>
    </div>
    <div class="form-group">
        <label for="precio" class="form-label">Precio</label>
        <input type="number" id="{{precio}}" name="precio" class="form-control"/>
    </div>
    <div class="form-group">
        <label for="forourl" class="form-label">Foto</label>
        <input type="text" id="{{forourl}}" name="forourl" class="form-control"/>
    </div>
    <button class="btn btn-success">Enviar</button>
    </form>
    </div>

    <div class="container">
    <h2 style="color:crimson;">Vista de Productos</h2>
    {{#if producto}}
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr style="color: yellow;"> 
                        <th>Nombre</th> <th>Precio</th> <th>Foto URL</th> 
                    </tr>
                </thead>
        <tbody>
            {{#for producto of productos}}
                <tr>
                    <td>{{{nombre}}}</td>
                    <td>{{{precio}}}</td>
                    <td>{{{fotourl}}}</td>
                </tr>
            {{/for}}
        </tbody>
            </table>
        </div>
    {{else}}
    <div class="alert alert-danger">No se encontraron productos</div>
    {{/if}}
    </div>
</body>
</html>